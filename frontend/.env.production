# Baked into the production build by `vite build`, so the deployed frontend
# talks to the live backend. Update this + rebuild if the backend URL changes.
VITE_API_BASE_URL=https://medintel-ai-k39i.onrender.com
