# MedIntel AI does NOT use Docker for deployment.
# The backend deploys on Render's native Python runtime (see render.yaml + render-build.sh).
# This file is intentionally left as a no-op. Safe to delete from your machine.
