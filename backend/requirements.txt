# MedIntel AI - backend runtime dependencies (production / deployment).
# Run the server from the PROJECT ROOT:  uvicorn backend.main:app
#
# NOTE on the heavy ML/RAG libs (torch, xgboost): the Dockerfile installs
# CPU-only PyTorch and xgboost --no-deps BEFORE this file so the image stays
# lean (no ~2GB CUDA payload). A plain `pip install -r` here still works for
# local dev; it will just pull the default wheels for your platform.

# --- Web API core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
python-multipart==0.0.20

# --- ML prediction (XGBoost v2 models via /predict + SHAP) ---
joblib>=1.3
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
xgboost==3.3.0          # pinned to match the saved v2 model pickles

# --- PDF report processing (/upload/report) ---
pymupdf>=1.24           # imported as "fitz"

# --- RAG + Gemini chat (/chat) ---
google-genai>=0.3
langchain-community>=0.3
langchain-huggingface>=0.1
sentence-transformers>=3.0   # pulls torch (Dockerfile pre-installs the CPU build)
faiss-cpu>=1.9

# --- Optional persistent history (used only if MONGODB_URI is set) ---
pymongo>=4.6
