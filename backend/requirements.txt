# MedIntel AI — backend runtime dependencies.
# Install into your virtual env:  pip install -r backend/requirements.txt
# (Run the server from the PROJECT ROOT: uvicorn backend.main:app --reload)

# --- Web API core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
python-multipart==0.0.20            # required by FastAPI for file uploads (/upload/report)

# --- ML prediction (loading + serving the XGBoost models via /predict) ---
# The trained .pkl models are XGBoost sklearn-wrapper objects, so scikit-learn
# and xgboost must be importable to unpickle them; joblib/numpy do the loading
# and the raw->standardized scaling.
joblib>=1.3
numpy>=1.26
scikit-learn>=1.3
xgboost>=2.0
pandas>=2.0                          # used by the scaler build + ML utilities

# --- PDF report processing (/upload/report) ---
pymupdf>=1.24                        # imported as "fitz"

# --- Optional: real RAG + Gemini chat pipeline (GenAI engineer) ---
# The /chat endpoint automatically uses a built-in offline responder unless
# these are installed AND a FAISS index + GEMINI_API_KEY are configured.
# Uncomment to enable the real pipeline:
# google-genai>=0.3
# langchain>=0.2
# langchain-community>=0.2
# faiss-cpu>=1.8
# sentence-transformers>=3.0
