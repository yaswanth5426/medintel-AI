# Copy to .env and fill in real values. Never commit the actual .env file.

# CORS: comma-separated extra origins allowed to call the API (frontend dev
# server is already allowed by default).
CORS_ORIGINS=

# Added once backend/database/mongodb.py is implemented.
MONGODB_URI=

# Owned by the GenAI engineer — used by backend/rag/, not by this branch.
GEMINI_API_KEY=
